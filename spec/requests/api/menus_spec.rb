 require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/api/menus", type: :request do
  # Api::Menu. As you add validations to Api::Menu, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      Api::Menu.create! valid_attributes
      get api_menus_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      menu = Api::Menu.create! valid_attributes
      get api_menu_url(api_menu)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_api_menu_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "render a successful response" do
      menu = Api::Menu.create! valid_attributes
      get edit_api_menu_url(api_menu)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new Api::Menu" do
        expect {
          post api_menus_url, params: { api_menu: valid_attributes }
        }.to change(Api::Menu, :count).by(1)
      end

      it "redirects to the created api_menu" do
        post api_menus_url, params: { api_menu: valid_attributes }
        expect(response).to redirect_to(api_menu_url(@api_menu))
      end
    end

    context "with invalid parameters" do
      it "does not create a new Api::Menu" do
        expect {
          post api_menus_url, params: { api_menu: invalid_attributes }
        }.to change(Api::Menu, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post api_menus_url, params: { api_menu: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested api_menu" do
        menu = Api::Menu.create! valid_attributes
        patch api_menu_url(api_menu), params: { api_menu: new_attributes }
        menu.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the api_menu" do
        menu = Api::Menu.create! valid_attributes
        patch api_menu_url(api_menu), params: { api_menu: new_attributes }
        menu.reload
        expect(response).to redirect_to(api_menu_url(menu))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        menu = Api::Menu.create! valid_attributes
        patch api_menu_url(api_menu), params: { api_menu: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested api_menu" do
      menu = Api::Menu.create! valid_attributes
      expect {
        delete api_menu_url(api_menu)
      }.to change(Api::Menu, :count).by(-1)
    end

    it "redirects to the api_menus list" do
      menu = Api::Menu.create! valid_attributes
      delete api_menu_url(api_menu)
      expect(response).to redirect_to(api_menus_url)
    end
  end
end
