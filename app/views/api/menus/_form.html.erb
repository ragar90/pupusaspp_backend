<%= form_with(model: api_menu, local: true) do |form| %>
  <% if api_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_menu.errors.count, "error") %> prohibited this api_menu from being saved:</h2>

      <ul>
        <% api_menu.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
